export interface IconItems {
    header: string;
    icons: Icon[];
}

export interface Icon {
    iconClass: string;
    iconLabel: string;
    iconUsage: string;
    iconISO: string;
    iconMeaning: string;
}
